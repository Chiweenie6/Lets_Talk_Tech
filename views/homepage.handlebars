<!-- Go through each object in the posts array -->
{{#each posts as |post|}}
  <div class="row mb-4 post d-flex justify-content-center">
    <div class="col-md-5 entry" id="">
      <h2>




       <a href="/posts/{{post.id}}">{{post.title}}</a>




        {{!-- {{#if logged_in}}
          <a href="/posts/{{post.id}}">{{post.title}}</a>
        {{else}}
          <a href="/login">Login 🎈</a>
        {{/if}} --}}
      </h2>
      <h6>Created by
        {{post.user.username}}
        on
        {{format_date post.date_created}}
      </h6>
      <div class="col-md-7">
        <p>
          {{post.content}}
        </p>
      </div>

    </div>

  </div>
{{/each}}